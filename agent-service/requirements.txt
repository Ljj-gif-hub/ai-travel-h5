# Agent 旅游规划服务依赖
# Python >= 3.10

# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.1.0

# LLM & Agent
langchain>=0.3.0
langchain-openai>=0.2.0
langgraph>=0.2.0

# 联网搜索
tavily-python>=0.5.0

# HTTP 客户端（调用高德地图 API）
httpx>=0.28.0

# 数据校验
pydantic>=2.0.0

# 环境变量
python-dotenv>=1.0.0

# MCP（Model Context Protocol）— 可选，未安装则 MCP 功能优雅降级
mcp>=1.24,<2
langchain-mcp-adapters>=0.3.1

# JWT 鉴权（与后端共享密钥校验 Bearer Token，绑定 user_id）
PyJWT>=2.8.0
