# 全库 Bug 审计报告

> 审计日期：2026-08-18 ｜ 覆盖范围：travel-java（202 文件）、trval-h5（126 文件）、agent-service（29 文件）
> 方式：14 个并行深度审查（控制器/安全、资金链、外部服务、内容社区、实体SQL、前端全局/页面、Python 服务）逐文件人工读源码
> 共 **136 项**确认 bug：P0 14 项、中危 56 项、低危 62 项（另有 4 项审查结论经复核为误报已剔除）
> 用法：逐项勾选 `[x]` 跟踪修复进度。优先级：P0 资金 → P0 可用性 → 地图泄漏 → 其余按模块批量处理。

---

## 一、P0 - 资金安全（4 项）

- [ ] **REFUND-1** [RefundService.java:87-118](travel-java/src/main/java/org/example/traveljava/service/RefundService.java#L87-L118) 退款审核无并发防护：`handle()` 是「读 refund 判 pending → 调渠道 → save」，无 CAS。两个管理员并发批准同一退款单 → 渠道退两次款，资金双倍退回。修复：仿 `markPaidIfPending` 做原子状态更新（`UPDATE ... WHERE status='pending'`）。
- [ ] **REFUND-2** [RefundService.java:56-58](travel-java/src/main/java/org/example/traveljava/service/RefundService.java#L56-L58) ①`requestRefund` 去重是先查后插，无 (order_id, pending) 唯一约束，并发可产生两条 pending 退款单；②`handle()` 打款前不校验该订单是否已退过款/是否仍为 paid → 管理员顺序批准两单也双倍退款。
- [ ] **ORDER-1** [OrderService.java:271-290](travel-java/src/main/java/org/example/traveljava/service/OrderService.java#L271-L290) 已支付订单可直接取消（paid→cancelled）但不触发退款；取消后 `requestRefund:52` 又要求必须是 paid 才能申请 → 付款永久无法退回，状态死锁。`updateOrderStatus:244-269` 同类（当前未暴露）。
- [ ] **COUPON-1** [CouponService.java:95-119](travel-java/src/main/java/org/example/traveljava/service/CouponService.java#L95-L119) `useCoupon` 读-改-写丢失更新：order 开头读取、`setPrice(price - discount)` 后全字段 merge。并发用两张券后者覆盖前者（前一张券已置 used 但抵扣消失）；与支付回调并发时旧实体（pending）merge 回写可把已支付订单打回 pending → 券已用、钱已收、订单待付。

## 二、P0 - 整站可用性 / 安全（6 项）

- [ ] **AVAIL-1** [RestTemplateConfig.java:12-14](travel-java/src/main/java/org/example/traveljava/config/RestTemplateConfig.java#L12-L14) RestTemplate 全局无 connect/read 超时（默认无限等待），被 Amap/Baidu/Weather/CityService 共用。外部端挂起 → 请求线程永久阻塞 → 耗尽 Tomcat 工作线程，整站无响应。修复：设 connectTimeout 3-5s、readTimeout 10s。
- [ ] **RATE-1** [RateLimitInterceptor.java:81-96](travel-java/src/main/java/org/example/traveljava/interceptor/RateLimitInterceptor.java#L81-L96) 线上 nginx 反代 + `app.rate-limit.trust-proxy` 默认 false → `getRemoteAddr()` 恒为 nginx 容器 IP，**全站用户合并成一个限流桶**。auth_login max=10/60s → 全站每分钟只能登录 10 次，高峰期全站假性 429 锁死。修复：线上设 `RATE_LIMIT_TRUST_PROXY=true` 并确认取 X-Forwarded-For 逻辑正确。
- [ ] **QUOTA-1** [AiQuotaInterceptor.java:60-67](travel-java/src/main/java/org/example/traveljava/interceptor/AiQuotaInterceptor.java#L60-L67) AI 每日配额只拦 `/api/agent/**`、`/api/travel/ai/**`、`/api/travel/stream/**`（第三个路径不存在）。真正烧钱的 `/api/travel/plan(/stream|/structured)`、`/api/travel/chat`、`/api/travel/planner/*`、`/api/trip/ai/**` 全部绕过配额 → DeepSeek 账单可被脚本刷爆。
- [ ] **AUTH-1** [router/index.js:298-302](trval-h5/src/router/index.js#L298-L302) 路由守卫见 access token 过期（JWT exp）即 `removeToken()` 踢回登录页，从不尝试用 REFRESH_TOKEN 刷新（axios 层的 401 单飞刷新被守卫抢先）→ 7 天免登录协议在「先导航后请求」场景全部失效；且 `removeToken()` 只清 access token，refresh token 残留。
- [ ] **FE-CRASH-1** [CommunityView.vue:544](trval-h5/src/views/CommunityView.vue#L544) `onDeactivated` 被调用但未从 vue import（第 2 行 import 列表缺失）→ 社区 Tab 一打开即 `ReferenceError`，整个社区页不可用。修复：补 import。
- [ ] **ABUSE-1** [CityController.java:155-185](travel-java/src/main/java/org/example/traveljava/controller/CityController.java#L155-L185) `/api/city/image` 未鉴权、无限流：缓存 miss（每个新 name 必 miss）即外呼高德 + 插一行 CityImage。攻击者循环 `?name=随机串` → 高德配额刷爆 + DB 无限膨胀。修复：加 @RateLimit + 登录校验或入参白名单。

## 三、P0 - 前端功能整体失效 / 重复支付（4 项）

- [ ] **MAP-1** [TripMapView.vue:765](trval-h5/src/views/TripMapView.vue#L765)（另 1069）`startGeneration` 里 `initMap(dest)` 异步（fetchMapProvider→SDK→200ms 轮询），紧接的 `loadMapMarkers(dest)` 同步执行，入口 `if (!mapInstance) return` 必命中 → 地图上永远只有城市标签，地标/地铁圆点一个不显示。修复：initMap 完成后再 loadMapMarkers。
- [ ] **CAL-1** [TripMapView.vue:1108-1121](trval-h5/src/views/TripMapView.vue#L1108-L1121)（配合 [stores/trip.js:94-117](trval-h5/src/stores/trip.js#L94-L117)）`goCalendar()` 只 push 路由不带 savedPlanId；旧组件先卸载触发 `store.resetState()` 清空 planData，日历页挂载后读到 null → 「暂无行程」空态，刚生成/保存的行程看不到。
- [ ] **PAY-1** [OrdersView.vue:59-76](trval-h5/src/views/OrdersView.vue#L59-L76) `handlePay` 无防重锁、按钮无 `:loading`，弱网双击并发 `createPayment`+`mockPay` → 两笔支付单（与 COUPON-1/PAYMENT-1 叠加成真实重复支付）。
- [ ] **ICS-1** [SavedTravelPlanService.java:251-259](travel-java/src/main/java/org/example/traveljava/service/SavedTravelPlanService.java#L251-L259) `foldLine`：`while (line.getBytes(UTF_8).length > 75)` 按字节判，`line.substring(0, 75)` 按字符截 → 40 个汉字（120 字节、40 字符）直接 `StringIndexOutOfBoundsException`。中文行程 DESCRIPTION 超约 25 个汉字即必现，`/api/trip-plans/{id}/ics` 导出 500。修复：`cut = Math.min(cut, line.length())` 且按字符回退。

---

## 四、中危 - 后端：订单支付链（8 项）

- [ ] **REFUND-3** [RefundService.java:98-118](travel-java/src/main/java/org/example/traveljava/service/RefundService.java#L98-L118) 事务内调 `refundProvider.refund()`（真实渠道为 HTTP）。渠道成功后 DB save/订单取消任一步失败 → 事务回滚 → 渠道已退款、DB 仍 pending → 重试二次退款。且远程调用期间长期占用 DB 连接。修复：渠道调用移出事务，或先 CAS 置 processing、成功后再落 refunded。
- [ ] **UNIT-1** 金额单位口径混乱：`Order.price` 及全链路实际为**元**（MockFlightProvider 报价、HotelService totalPrice、CouponService 日志），但 [RefundProvider.java:15](travel-java/src/main/java/org/example/traveljava/service/RefundProvider.java#L15)、[Refund.java:37](travel-java/src/main/java/org/example/traveljava/entity/Refund.java#L37)、[Invoice.java:45](travel-java/src/main/java/org/example/traveljava/entity/Invoice.java#L45)、[MockRefundProvider.java:28](travel-java/src/main/java/org/example/traveljava/service/MockRefundProvider.java#L28) 注释全声称**分**。按注释接真实渠道（支付宝/微信以分为单位）即 100 倍资损。修复：统一注释与接口契约为元，接入层做显式换算。
- [ ] **COUPON-2** [CouponService.java:100-119](travel-java/src/main/java/org/example/traveljava/service/CouponService.java#L100-L119) 不校验 `order.getCouponId() == null`，同一订单可无限叠加券；couponId/couponValue 只留最后一张 → 抵扣明细丢失，发票/退款/审计无法追溯。
- [ ] **PAYMENT-1** [PaymentService.java:46-56](travel-java/src/main/java/org/example/traveljava/service/PaymentService.java#L46-L56) createPayment 事务内远程调用 + 旧实体全字段 merge：并发回调在读取与 save 之间置 paid → merge 用 pending 覆盖回去（已收款订单回退待支付）；并发 createPayment 互相覆盖 payTradeNo，无幂等。
- [ ] **COUPON-3** [CouponRepository.java:23-26](travel-java/src/main/java/org/example/traveljava/repository/CouponRepository.java#L23-L26)（+[CouponService.java:121](travel-java/src/main/java/org/example/traveljava/service/CouponService.java#L121)）`@Modifying` 无 `clearAutomatically`，bulk update 后 `findById` 命中一级缓存旧实体 → 返回前端 status 仍 unused。
- [ ] **INVOICE-1** [InvoiceService.java:84-95](travel-java/src/main/java/org/example/traveljava/service/InvoiceService.java#L84-L95) 发票号 `existsByInvoiceNo` 先查后插非原子，并发可同号；重试 10 次后的纳秒兜底同样可冲突 → 唯一约束 500。`issue()` 的 existsByOrderId 前置检查并发下靠 DB 约束兜底但异常未转友好提示。
- [ ] **PAYMENT-2** [PaymentService.java:76-90](travel-java/src/main/java/org/example/traveljava/service/PaymentService.java#L76-L90) `handleNotify` 只取 orderNo 不比对回调金额与订单金额（当前 RealPaymentProvider 验签 fail-closed 未触雷；接真实渠道时 1 分钱回调可标记整单已付）。
- [ ] **COST-1** [CostCalculationService.java:196-214](travel-java/src/main/java/org/example/traveljava/service/CostCalculationService.java#L196-L214) 豪华/经济偏好只对 hotelCost 乘系数却对 totalCost（含旧酒店费）整体乘 → 分项之和 ≠ 总额；乘后未重新 `setScale(2)`。

## 五、中危 - 后端：控制器/安全（6 项）

- [ ] **CTRL-1** [TravelAIController.java:105](travel-java/src/main/java/org/example/traveljava/controller/TravelAIController.java#L105)、[:128](travel-java/src/main/java/org/example/traveljava/controller/TravelAIController.java#L128) `log.info("...消息数={}", messages.size())` 先于 `if (messages == null)` → body 为字面量 null 即 NPE 500。判空代码形同虚设。
- [ ] **CTRL-2** Controller 层普遍 `((Number) x).intValue()` 强转，JSON 传字符串数字即 ClassCastException 500（而非 400）：[TripAIController.java:44](travel-java/src/main/java/org/example/traveljava/controller/TripAIController.java#L44)/[73](travel-java/src/main/java/org/example/traveljava/controller/TripAIController.java#L73)/[147](travel-java/src/main/java/org/example/traveljava/controller/TripAIController.java#L147)、[TravelController.java:137-243](travel-java/src/main/java/org/example/traveljava/controller/TravelController.java#L137-L243)、[NoteCollectionController.java:108](travel-java/src/main/java/org/example/traveljava/controller/NoteCollectionController.java#L108)、[ShareController.java:43](travel-java/src/main/java/org/example/traveljava/controller/ShareController.java#L43)、[PaymentController.java:51](travel-java/src/main/java/org/example/traveljava/controller/PaymentController.java#L51)、[ReportController.java:47](travel-java/src/main/java/org/example/traveljava/controller/ReportController.java#L47)。
- [ ] **CTRL-3** [HotelController.java:149-151](travel-java/src/main/java/org/example/traveljava/controller/HotelController.java#L149-L151) `LocalDate.parse((String) body.get("checkIn"))`：null→NPE、数字→CCE、格式错→DateTimeParseException，全部落入笼统「预订失败」；且允许 2000-01-01 等历史日期下单。
- [ ] **UPLOAD-1** [FileUploadController.java:101](travel-java/src/main/java/org/example/traveljava/controller/FileUploadController.java#L101)、[:125](travel-java/src/main/java/org/example/traveljava/controller/FileUploadController.java#L125) part 无 Content-Type 时 `getContentType()` 为 null：`IMAGE_TYPES.contains(null)`（Set.of）与 `MIME_TO_EXT.getOrDefault(null,...)`（Map.of）均抛 NPE 500。
- [ ] **EXH-1** [GlobalExceptionHandler.java:116-124](travel-java/src/main/java/org/example/traveljava/config/GlobalExceptionHandler.java#L116-L124) `@ExceptionHandler({ClientAbortException.class, IOException.class})` 范围过宽：控制器内真实 IO 故障也被吞 → 响应空 body 的 200，前端把「成功但无数据」当正常，掩盖故障。应只捕 ClientAbortException。
- [ ] **RATE-2** [RateLimitInterceptor.java:56-59](travel-java/src/main/java/org/example/traveljava/interceptor/RateLimitInterceptor.java#L56-L59) INCR 与 EXPIRE 非原子：increment 成功后 expire 抛异常（Redis 抖动）→ key +1 但永不过期 → 该桶永久 429，重启 Redis 才恢复。改 Lua 或 `SET NX EX`。

## 六、中危 - 后端：AI/地图/天气（7 项）

- [ ] **AMAP-1** [BaiduMapService.java:90-95](travel-java/src/main/java/org/example/traveljava/service/BaiduMapService.java#L90-L95)（geocode）、[:134-142](travel-java/src/main/java/org/example/traveljava/service/BaiduMapService.java#L134-L142)（suggestions）、[:763-772](travel-java/src/main/java/org/example/traveljava/service/BaiduMapService.java#L763-L772)（fetchRealImage）三处 `toUriString()` 后把 String 传 `restTemplate.getForObject` → `%`→`%25` 二次编码，中文参数失效（e750fb3 同类坑遗漏面；当前百度 AK 未配未触发，`map.provider=baidu` 是默认值，配 AK 瞬间全中招）。修复：改用 `URI` 传参。
- [ ] **SSE-1** [TravelSseService.java:184-204](travel-java/src/main/java/org/example/traveljava/service/TravelSseService.java#L184-L204)（+[TravelController.java:71-72](travel-java/src/main/java/org/example/traveljava/controller/TravelController.java#L71-L72)）`subscribePlannerTrip` 的 Disposable 被丢弃、onCompletion/onTimeout 只打日志不 dispose → 客户端断开后上游 WebClient 继续从 DeepSeek 拉完整响应（最长 120s），白烧 token、占连接；反复断连累积僵尸流。
- [ ] **WEATHER-1** [WeatherService.java:80](travel-java/src/main/java/org/example/traveljava/service/WeatherService.java#L80)（+[123-133](travel-java/src/main/java/org/example/traveljava/service/WeatherService.java#L123-L133)）请求 `extensions=all`（返回 forecasts）却解析 `lives`（base 才有）→ city/weather/temperature/windDirection 等实时字段永远缺失，前端「当前天气」恒为空。
- [ ] **REC-1** [RecommendationService.java:359](travel-java/src/main/java/org/example/traveljava/service/RecommendationService.java#L359)、[:366-375](travel-java/src/main/java/org/example/traveljava/service/RecommendationService.java#L366-L375) 候选池内 `userKeysCache = new HashMap<>()` 被 Caffeine 缓存后所有请求线程共享，`userKeys()` 无锁 put → 并发写 HashMap。改 ConcurrentHashMap。
- [ ] **AI-1** [AIService.java:756](travel-java/src/main/java/org/example/traveljava/service/AIService.java#L756) cancelFlags 无界 ConcurrentHashMap，仅 [825](travel-java/src/main/java/org/example/traveljava/service/AIService.java#L825)（streamPlannerWithStages）一处 remove；detail/单端点任务取消后标志永久残留，慢性泄漏。
- [ ] **FLIGHT-1** [FlightService.java:67](travel-java/src/main/java/org/example/traveljava/service/FlightService.java#L67) `((Number) params.get("passengers")).intValue()` 只判 null，传字符串 `"2"` 即 CCE 500（应 400）。
- [ ] **CITYMAT-1** [CityMaterialService.java:251-284](travel-java/src/main/java/org/example/traveljava/service/CityMaterialService.java#L251-L284) `refreshAllPhotos`：①254 行先 `deleteAll()`，百度 provider 下 amapService 为 null → NPE 被 catch 以 debug 吞掉 → 城市图片表被清空且一个不写回；②~140 城 × (HTTP+300ms) 在请求线程串行执行数分钟必超时；③无事务/幂等。另 [222 行](travel-java/src/main/java/org/example/traveljava/service/CityMaterialService.java#L222) 启动预取用非守护裸线程独占高德限速桶 2-4 分钟（期间用户地图请求排队）、拖延 JVM 退出。

## 七、中危 - 后端：内容社区/MQ（5 项）

- [ ] **LIKE-1** [NoteService.java:184-193](travel-java/src/main/java/org/example/traveljava/service/NoteService.java#L184-L193)、[PostService.java:181-187](travel-java/src/main/java/org/example/traveljava/service/PostService.java#L181-L187)、[CommentService.java:183-191](travel-java/src/main/java/org/example/traveljava/service/CommentService.java#L183-L191) 点赞并发兜底是假的：IDENTITY 主键 save 即 INSERT、唯一约束冲突立刻抛且已标记 rollback-only，catch `DataIntegrityViolationException` 后同事务继续 `findById`/提交 → `UnexpectedRollbackException`，双击点赞得 500 而非「已点赞」。
- [ ] **REPORT-1** [ReportService.java:78](travel-java/src/main/java/org/example/traveljava/service/ReportService.java#L78) 自动隐藏阈值统计含**已驳回**（STATUS_IGNORED）举报：5 人举报被驳回后，任意 1 人再举报 total=6≥5 → 再次自动隐藏，管理员驳回决定被一条新举报推翻，举报可被武器化打压正常内容。计数应过滤驳回。
- [ ] **COMMENT-1** [CommentService.java:62](travel-java/src/main/java/org/example/traveljava/service/CommentService.java#L62)（getTopReply）、[:88](travel-java/src/main/java/org/example/traveljava/service/CommentService.java#L88)（getTopReplies）热评预览用 `findByParentId`（无 hidden 过滤），被举报隐藏的回复仍从「热评预览」泄露（getReplies:57 有过滤）。
- [ ] **COLL-1** [NoteCollectionService.java:101-110](travel-java/src/main/java/org/example/traveljava/service/NoteCollectionService.java#L101-L110)、[:125-129](travel-java/src/main/java/org/example/traveljava/service/NoteCollectionService.java#L125-L129) 收藏夹 noteIds JSON 数组纯读-改-写，无乐观锁/唯一约束 → 并发添加后提交者覆盖先提交者，静默丢失。
- [ ] **MQ-1** [OrderPaidEventConsumer.java:47-57](travel-java/src/main/java/org/example/traveljava/mq/OrderPaidEventConsumer.java#L47-L57)（+[RabbitEventPublisher.java:38-46](travel-java/src/main/java/org/example/traveljava/mq/RabbitEventPublisher.java#L38-L46)）消费者注释宣称「异常自动重投」，实际 `catch (Exception)` 吞掉一切照常 ACK → 事件丢失；无 eventId 幂等，真 redelivery 会重复插审计行。发布端 convertAndSend 失败仅 WARN 即丢弃，无 confirm/outbox/重试。

## 八、中危 - SQL/配置（3 项）

- [ ] **SQL-1** [V3__feature_2026_08.sql:135-137](travel-java/src/main/resources/db/migration/V3__feature_2026_08.sql#L135-L137)、[:144](travel-java/src/main/resources/db/migration/V3__feature_2026_08.sql#L144) `ALTER TABLE ... ADD COLUMN`（notes/posts/comments.hidden、users.points）无幂等保护，前面 CREATE TABLE 均带 IF NOT EXISTS 而 ALTER 不支持 → 脚本在 135 行后任一处失败，重跑必报 `Duplicate column name`，卡中途状态。生产执行 V3 前先核对列是否已存在。
- [ ] **SQL-2** [CityDTO.java:54](travel-java/src/main/java/org/example/traveljava/dto/CityDTO.java#L54)、[:138](travel-java/src/main/java/org/example/traveljava/dto/CityDTO.java#L138) 兜底图拼 `source.unsplash.com`（该服务已停服）且城市名中文未编码 → 走此构造器的城市卡片/搜索结果全裂图。
- [ ] **SQL-3** [V2__trip_map_init.sql:65-206](travel-java/src/main/resources/db/migration/V2__trip_map_init.sql#L65-L206) 使用 H2 专有语法（MERGE INTO ... KEY(id)、CLOB），与 V3 的 MySQL 方言混在同一迁移目录且项目无 Flyway → 新环境按序在 MySQL 重放迁移链，V2 第一条 MERGE 即报语法错误。

## 九、中危 - agent-service/Python（6 项）

- [ ] **PY-1** [agent/tools.py:300-308](agent-service/agent/tools.py#L300-L308) 公交路径规划漏传必填参数 `city`（函数 230 行收了参数却没传给 `direction/transit/integrated`）→ status=0，公交模式通勤查询**永远失败**（驾车/步行正常）。
- [ ] **PY-2** [agent/tools.py:297](agent-service/agent/tools.py#L297)、[:312-330](agent-service/agent/tools.py#L312-L330) 骑行用 v4 接口（`v4/direction/bicycling`，响应为 `{errcode, data.paths}`）却按 v3 结构（`status/route.paths`）解析 → 骑行模式**永远走失败分支**。
- [ ] **PY-3** [main.py:427-479](agent-service/main.py#L427-L479)、[:522-570](agent-service/main.py#L522-L570) 两个主 SSE 流用裸 StreamingResponse 无心跳；research 阶段 3 个并行子智能体 + 汇总期间静默可达 60-120s+，nginx `proxy_read_timeout` 默认 60s → 规划中途断流。错误路径的 EventSourceResponse 反而自带 15s ping。修复：加周期 ping 事件或 nginx 调大超时。
- [ ] **PY-4** [agent/auth.py:118](agent-service/agent/auth.py#L118) `hmac.compare_digest(key, provided)` 遇 latin-1 非 ASCII 的 X-Agent-Key 抛 TypeError 未捕获 → 500（fail-closed 无越权，但外部可轻易触发刷错误日志）。修复：先 `provided.encode('utf-8')` 比较字节。
- [ ] **PY-5** [main.py:300-306](agent-service/main.py#L300-L306) `submit_feedback` 在事件循环协程内同步做全量 JSON 序列化 + 写盘（planner 侧同类操作已用 `asyncio.to_thread`，此处漏）→ 落盘期间卡住所有进行中的 SSE 规划流，记忆文件越大越久（见 PY-6）。
- [ ] **PY-6** [agent/memory.py:109-120](agent-service/agent/memory.py#L109-L120)、[:135-144](agent-service/agent/memory.py#L135-L144) users/sessions 只增不删（feedback 有上限、research cache 有 LRU，唯独这两个没有），session_id 客户端任意提供；每次写入全量序列化整个文件落盘 → 文件可被刷到任意大，之后每次规划请求（每轮写 2 次）延迟持续恶化。

## 十、中危 - 前端：地图 / OOM（7 项，历史病灶）

- [ ] **MAPLEAK-1** [TripMapView.vue:364-368](trval-h5/src/views/TripMapView.vue#L364-L368)、[:523-527](trval-h5/src/views/TripMapView.vue#L523-L527) 引擎切换销毁错配：`initLeafletMap` 只调 `remove()`（AMap 没有）、`initAmapMap` 只调 `destroy()`（Leaflet 没有），异常被 catch 吞 → 反复切换「离线」胶囊每次泄漏一个完整地图实例（WebGL 上下文/瓦片/监听）+ 容器残留 DOM 叠影。卸载主路径 1116-1117 行做对了（双判断），仅切换路径泄漏。**与历史 OOM 同根因**。
- [ ] **MAPXSS-1** [TripMapView.vue:549-565](trval-h5/src/views/TripMapView.vue#L549-L565)、[:571-581](trval-h5/src/views/TripMapView.vue#L571-L581) `addCityLabelAmap`（AMap.Text 按 HTML 渲染）与 `addCityLabelLeaflet`（divIcon html）直接内插 `${city}`，city 来自 `route.query.destination`（URL 可控）→ XSS。AgentMapView 同场景已用 escapeHtml，此处遗漏。
- [ ] **MAPLEAK-2** [TripsView.vue:121](trval-h5/src/views/TripsView.vue#L121)、[:316](trval-h5/src/views/TripsView.vue#L316) 附近地图弹层 `closeNearbyMap`/`onUnmounted` 对 nearbyMapInstance 一律 `.destroy()`，Leaflet 只有 `.remove()`，异常被吞 → 每次开关弹层泄漏实例 + OSM 瓦片层监听。
- [ ] **MAPLEAK-3** [TripsView.vue:313](trval-h5/src/views/TripsView.vue#L313)（+[357](trval-h5/src/views/TripsView.vue#L357)）keep-alive 迷你地图：`onDeactivated` 销毁后 `miniMapReady` 不复位、`onActivated` 不重建（只在 onMounted 初始化）→ 无行程用户切 Tab 再回来，地图区域永久空白灰块（loading 占位也不再显示）。
- [ ] **MAPRACE-1** [AgentMapView.vue:323-348](trval-h5/src/views/AgentMapView.vue#L323-L348) `addMarkers` 入口守卫在 `await Promise.all(geocodeName)` 之前，卸载后 `destroyMap()` 置 null，await 恢复后 `mapInstance.add(mk)` TypeError（未捕获 rejection）。
- [ ] **SSERACE-1** [AgentMapView.vue:516](trval-h5/src/views/AgentMapView.vue#L516)（submitAdjust/491）、[TripMapView.vue:780](trval-h5/src/views/TripMapView.vue#L780) 重新生成前不调用旧 `streamAbort()`/`abortSSE()` 直接覆盖引用 → 旧 SSE 流仍存活，晚到的 onComplete 用旧 planData/stepList 覆盖新一轮结果。
- [ ] **MAPFAIL-1** [AgentMapView.vue:253-257](trval-h5/src/views/AgentMapView.vue#L253-L257) `/api/map/script` 按后端 provider 返回 SDK，本页只认 `window.AMap` → 后端切百度 provider 后本页永远停在假渐变背景（当前线上高德未暴露）。
- [ ] **MAPLEAK-4** [TripsView.vue:157-163](trval-h5/src/views/TripsView.vue#L157-L163) `changeRadius` 只在 amap 分支 clearMap，Leaflet 分支旧 circleMarker 不清直接叠加 → 3km→5km→10km 三层标记堆叠。

## 十一、中危 - 前端：跨账号数据残留（2 项）

- [ ] **ACCT-1** [Profile.vue:334-349](trval-h5/src/views/Profile.vue#L334-L349) 登出不调 `store.resetState()`（模块级单例 planData/costBreakdown/params）→ 账号 A 生成行程后登出，账号 B 登录即见 A 的行程（日历页、agent-map 均读旧 planData）。
- [ ] **ACCT-2** [Profile.vue:373](trval-h5/src/views/Profile.vue#L373)、[:381](trval-h5/src/views/Profile.vue#L381)、[EditProfileView.vue:57](trval-h5/src/views/EditProfileView.vue#L57)、[:92](trval-h5/src/views/EditProfileView.vue#L92) 全局 `localStorage.userInfo` 登出不清；当前账号 profile 接口失败时兜底读它 → B 账号显示 A 的昵称/头像/积分。

## 十二、中危 - 前端：重复操作 / 竞态（11 项）

- [ ] **DBLCLICK-1** [HotelBookingView.vue:81-100](trval-h5/src/views/HotelBookingView.vue#L81-L100)、[FlightBookingView.vue:92-115](trval-h5/src/views/FlightBookingView.vue#L92-L115) 预订确认无提交锁无 `:loading` → 弱网双击创建两笔重复订单。
- [ ] **PAGE-1** [HomeView.vue:410-444](trval-h5/src/views/HomeView.vue#L410-L444)（+847-861）首页笔记：reset 加载期间 `loadingMore=false`，触底再触发非 reset 加载 → 两个并发请求都拉第 1 页 → 数据重复追加、page 跳到 3、第 2 页被永久跳过。
- [ ] **PAGE-2** [CommunityView.vue:482-489](trval-h5/src/views/CommunityView.vue#L482-L489)（+165-177）同模式：reload 期间触底再拉一遍 page=1 → 列表重复卡片。
- [ ] **TAB-1** [OrdersView.vue:33-56](trval-h5/src/views/OrdersView.vue#L33-L56)、[FavoritesView.vue:33-56](trval-h5/src/views/FavoritesView.vue#L33-L56)、[CouponsView.vue:33-56](trval-h5/src/views/CouponsView.vue#L33-L56) Tab 快速切换无请求守卫 → 慢响应晚到覆盖当前 Tab 列表（点酒店→全部，显示的却是酒店）。
- [ ] **RACE-1** [VideoDetailView.vue:237-257](trval-h5/src/views/VideoDetailView.vue#L237-L257) loadComments 无 seq/Abort 守卫，快速滑切视频 → A 的评论覆盖 B 的评论区（含回复点赞对象错乱）。
- [ ] **RACE-2** [ChatView.vue:283](trval-h5/src/views/ChatView.vue#L283) 流式追加 `messages.value[aiMsgIndex].content += ...` 无存在性守卫（同函数 293/333/337 都有）：流中清空对话/恢复历史 → TypeError 被 catch 当网络错误 → 退避重连用旧 chatHistory 重发 → 再次 TypeError → 弹「请求失败」。
- [ ] **RACE-3** [PlanningView.vue:945-999](trval-h5/src/views/PlanningView.vue#L945-L999) 180s 生成请求的 AbortController 未绑定组件卸载 → 人已离开仍弹全局「规划完成」toast + 对全部 pending 景点逐个发 sceneImage 后台请求。
- [ ] **RACE-4** [AITripPlannerProgress.vue:79-103](trval-h5/src/views/AITripPlannerProgress.vue#L79-L103) SSE 初连失败进重试循环，2s sleep 期间卸载 → 循环继续 `new AbortController()` 发新连接且无人能 abort；[121-127](trval-h5/src/views/AITripPlannerProgress.vue#L121-L127) generate-finish 的 `setTimeout(router.push('/planning'))` 不随停止/卸载取消 → 强制跳转劫持用户导航。
- [ ] **RACE-5** [AITripPlannerProgress.vue:144-175](trval-h5/src/views/AITripPlannerProgress.vue#L144-L175) ①拿到 taskId 前点停止/返回：`/planner/stop` 因 taskId 空不调用，后端跑完整 5 阶段白烧 LLM；②taskId 仅存组件 ref，返回重进（或刷新）用相同入参重新 POST → 从头全新生成再次消耗配额，而非按 taskId 续传。
- [ ] **COMP-1** [LazyImage.vue:55-65](trval-h5/src/components/LazyImage.vue#L55-L65)、[:72-79](trval-h5/src/components/LazyImage.vue#L72-L79) 不响应 src prop 变化：挂载时 src 为空则 observer 永不注册、后续 src 到达永不加载；failed 置 true 后无 reset → 换新地址仍显示错误占位（NoteDetailView 轮播复用即中招）。
- [ ] **COMP-2** [VirtualList.vue:121-132](trval-h5/src/components/VirtualList.vue#L121-L132) 动态高度模式卸载清理按 index 反查：同 index 新旧元素交替时旧元素清理错删新元素并 unobserve → 高度不实测修正，长列表滚动漂移。
- [ ] **COMP-3** [useRequest.js:66](trval-h5/src/composables/useRequest.js#L66) `run` catch 中 `throw e` 重抛，非 manual 自动首跑失败 → unhandled rejection；模板直接绑 `@click="refresh"` 同理（当前调用侧均 manual+catch 兜住，属封装层潜伏陷阱）。

## 十三、中危 - 前端：功能失效（13 项）

- [ ] **FEAT-1** [FeedbackView.vue:110-115](trval-h5/src/views/FeedbackView.vue#L110-L115) van-radio props 用错（Vant4 取值是 `name`、文案走默认插槽，此处写成 `:value`/`:label`）→ 四个选项无文字、`feedbackForm.type` 恒 undefined，**反馈提交整体不可用**（对照 OrdersView.vue:360 正确用法）。
- [ ] **FEAT-2** [CouponsView.vue:58-68](trval-h5/src/views/CouponsView.vue#L58-L68) 「立即使用」`useCoupon(coupon.id, null)` 传 null 订单号 → 后端必抛「订单不存在」，按钮形同虚设。应跳转下单流程携带 orderId。
- [ ] **FEAT-3** [NotesView.vue:45-57](trval-h5/src/views/NotesView.vue#L45-L57) 删除游记无确认弹窗且无锁 → 误触即永久删除整篇；双击发两次请求。
- [ ] **FEAT-4** [OrdersView.vue:78-88](trval-h5/src/views/OrdersView.vue#L78-L88) 取消订单无二次确认 → 误触直接 cancelled 不可恢复。
- [ ] **FEAT-5** [HomeView.vue:833-838](trval-h5/src/views/HomeView.vue#L833-L838) 城市选择器 `document.querySelector('.van-popup')` 取文档第一个 popup（Vant 懒渲染但打开过即常驻）→ 先开过其他弹窗后滚轮监听挂错对象，桌面鼠标滚轮选城市失效。
- [ ] **FEAT-6** [CommunityView.vue:746](trval-h5/src/views/CommunityView.vue#L746) 评论图片上传按钮 `e.target.parentElement.querySelector('input[type=file]')`：input 是兄弟节点非子节点，点在图标上（26px 圆钮内 16px 图标占大部分）找不到 → 点击大面积无效。改用 ref 或 closest。
- [ ] **FEAT-7** [DestinationDetailView.vue:193](trval-h5/src/views/DestinationDetailView.vue#L193)（对照 153-165）`getImageUrl(city)` 在 setup 同步求值，此刻 staticImageMap 尚空 → 主城市图永远走外部生图 API，本地图片库兜底永不生效（API 慢/失败时用户只见占位图）。
- [ ] **FEAT-8** [CitySelectView.vue:119](trval-h5/src/views/CitySelectView.vue#L119)、[AttractionSelectView.vue:46](trval-h5/src/views/AttractionSelectView.vue#L46) `selectCity` 依赖 `router.back()` 无 history 兜底（同文件 goBack 有）→ 刷新/深链进入后点城市「没反应」。
- [ ] **FEAT-9** [FavoritesView.vue:87-92](trval-h5/src/views/FavoritesView.vue#L87-L92)、[DestinationsView.vue:83-87](trval-h5/src/views/DestinationsView.vue#L83-L87) 封面兜底把第三方 AI 生图 GET 接口当 `<img src>` 直连 → 大概率裂图（DestinationsView @error 是 opacity:0 整卡空白）且把标题拼进 prompt 外发第三方域名。
- [ ] **FEAT-10** [CommunityView.vue:336-347](trval-h5/src/views/CommunityView.vue#L336-L347) keep-alive 缓存页视频从不 pause（toggleVideo 只去 controls、切页/进详情不停流）→ 缓存页后台持续拉流耗流量电量；A→B 切换 A 仍在播。
- [ ] **FEAT-11** [VideoDetailView.vue:543-549](trval-h5/src/views/VideoDetailView.vue#L543-L549) `<video autoplay>` 无 muted，iOS/Android 拒绝带声自动播放后 play() reject 未 catch、isPlaying 无条件置 true → 状态错乱画面停帧。
- [ ] **FEAT-12** [AIChatDialog.vue:326-334](trval-h5/src/components/AIChatDialog.vue#L326-L334) 构造的上下文 `prompt` 变量是死代码（chatHistory 直接从原始 content 映射，system 消息又被 filter 排除）→ 首页携带的目的地/预算/天数上下文 AI 完全收不到，回答与裸提问一致。
- [ ] **FEAT-13** [UploadFile](trval-h5/src/api/index.js#L339-L353) uploadFile 绕过整个 request 封装：不查 response.ok、无 401 刷新重放、413 HTML 错误页直接 json() 抛 `Unexpected token '<'` → 弱网/超限上传只得到无意义报错。

---

## 十四、低危 - 后端（19 项）

- [ ] **L-AI-1** [AIService.java:239](travel-java/src/main/java/org/example/traveljava/service/AIService.java#L239) testConnection `getChoices().get(0).getMessage().getContent()` 无 null 防护（AI 返回空 message 即 NPE 500）；[:480](travel-java/src/main/java/org/example/traveljava/service/AIService.java#L480) streamTravelPlan 同类（被 onErrorResume 吞成整体失败）。
- [ ] **L-AI-2** [AIService.java:318](travel-java/src/main/java/org/example/traveljava/service/AIService.java#L318) chat 缓存键 `messages.toString()` 无 userId → 两个用户相同提问共享同一缓存回复（行程缓存 252/520 行都加了 userId，chat 漏了）；且键含全量历史体积大。
- [ ] **L-AI-3** [AIService.java:1246-1255](travel-java/src/main/java/org/example/traveljava/service/AIService.java#L1246-L1255) 景点图键 `"image_"+景点名` 写入 planCache（1000 条/1h）→ 用户可控的任意景点名把真正行程缓存 LRU 挤掉。
- [ ] **L-MAP-1** [BaiduMapService.java:435-442](travel-java/src/main/java/org/example/traveljava/service/BaiduMapService.java#L435-L442)、[:599-608](travel-java/src/main/java/org/example/traveljava/service/BaiduMapService.java#L599-L608)、[:642-649](travel-java/src/main/java/org/example/traveljava/service/BaiduMapService.java#L642-L649) 手写 HttpURLConnection 无 try/finally，读流中途异常 → reader/conn 泄漏、不消费 errorStream。
- [ ] **L-MAP-2** [AmapService.java:60-66](travel-java/src/main/java/org/example/traveljava/service/AmapService.java#L60-L66) rateLimit 的 InterruptedException 分支直接 return → 未获令牌仍发起调用，限速被旁路；等待循环无上限。
- [ ] **L-AUD-1** [AuditService.java:41-66](travel-java/src/main/java/org/example/traveljava/service/AuditService.java#L41-L66) 单线程池无界队列，DB 长期不可达时任务只增不减，无背压。
- [ ] **L-CTRL-1** [TripAIController.java:99-107](travel-java/src/main/java/org/example/traveljava/controller/TripAIController.java#L99-L107) tripChat 的 history 未检查强转 + for 在 try 外：`"history":"abc"` 或元素混入数字 → CCE 500；元素缺 role/content → null role 消息传给 LLM。
- [ ] **L-CTRL-2** [TripAIController.java:186](travel-java/src/main/java/org/example/traveljava/controller/TripAIController.java#L186) `emitter.send("❌ "+e.getMessage())` 把含内网 URL/代理地址的上游异常 message 推给客户端（同文件其他端点均已改固定文案）。
- [ ] **L-CTRL-3** [TravelAIController.java:94-98](travel-java/src/main/java/org/example/traveljava/controller/TravelAIController.java#L94-L98) Flux+text/event-stream 手拼 `data:` 前缀与 Spring 编码器叠加成 `data:data:`（AgentProxyController:202-211 注释已记录同坑并修复了 Agent 侧）→ 当前无前端消费此端点，接入必现。
- [ ] **L-CTRL-4** [MapController.java:296-305](travel-java/src/main/java/org/example/traveljava/controller/MapController.java#L296-L305) landmarkCache/metroCache 无界 ConcurrentHashMap，key（city）攻击者可控 + 每新 city 触发外呼 → 长期内存缓慢泄漏 + 高德配额被刷。
- [ ] **L-CTRL-5** [ImageProxyController.java:88-97](travel-java/src/main/java/org/example/traveljava/controller/ImageProxyController.java#L88-L97)、[:182-205](travel-java/src/main/java/org/example/traveljava/controller/ImageProxyController.java#L182-L205) 防 DNS rebinding TOCTOU：校验后 execute 重新解析域名可被切到内网；内网段漏 100.64/10、0.0.0.0/8、198.18/15、多播。
- [ ] **L-CTRL-6** [TravelController.java:157-166](travel-java/src/main/java/org/example/traveljava/controller/TravelController.java#L157-L166)、[:341-351](travel-java/src/main/java/org/example/traveljava/controller/TravelController.java#L341-L351) stop/cancel/progress 接口不校验 taskId 归属（taskId 为 UUID 前 8 位）→ 跨用户停他人任务/接管事件流的低概率越权面。
- [ ] **L-SQL-1** [SqlTimingInspector.java:29-50](travel-java/src/main/java/org/example/traveljava/config/SqlTimingInspector.java#L29-L50) inspect 在绑定前调用：elapsed 是上一条间隔、日志打印下一条 SQL → 慢 SQL 日志张冠李戴；首条语句永不统计；ThreadLocal 不清理。
- [ ] **L-ENT-1** [AsyncAudit.java:32-33](travel-java/src/main/java/org/example/traveljava/entity/AsyncAudit.java#L32-L33) created_at NOT NULL 但无 @PrePersist 兜底（全项目唯一漏的实体），新调用点忘 setCreatedAt 即插库失败。
- [ ] **L-ENT-2** [V3__feature_2026_08.sql:31-32](travel-java/src/main/resources/db/migration/V3__feature_2026_08.sql#L31-L32) trip_shares.token 上 UNIQUE KEY 与普通 KEY 覆盖同列，后者纯冗余（[TripShare.java:12](travel-java/src/main/java/org/example/traveljava/entity/TripShare.java#L12) 同步声明）。
- [ ] **L-CFG-1** [application-postgres.yml:15](travel-java/src/main/resources/application-postgres.yml#L15) 硬编码默认口令 `${DB_PASSWORD:postgres}`，忘注入即静默用默认口令连库。
- [ ] **L-CFG-2** [application-mysql.yml:12](travel-java/src/main/resources/application-mysql.yml#L12)、[application-prod.yml:9](travel-java/src/main/resources/application-prod.yml#L9) `useSSL=false`，跨主机部署时 DB 凭据明文传输。
- [ ] **L-CFG-3** [application.yml:18-21](travel-java/src/main/resources/application.yml#L18-L21) `spring.http.encoding.*` 是 Boot2 死配置（Boot3 应为 server.servlet.encoding），无效键误导调参。
- [ ] **L-SHARE-1** [ShareService.java:38-63](travel-java/src/main/java/org/example/traveljava/service/ShareService.java#L38-L63) 永久分享无撤销/过期机制，`deletePlan` 不级联删 ShareRecord/TripShare → 删行程后分享链接命中 `RuntimeException("规划不存在，id=x")` 向公网泄露内部主键。
- [ ] **L-TOKEN-1** [UserService.java:171-183](travel-java/src/main/java/org/example/traveljava/service/UserService.java#L171-L183) logout 只撤销请求携带的一个 refreshToken，login 不吊销旧的、无 revoke-all → 同账号无限累积并行有效的 7 天刷新令牌，被盗后用户无全局吊销手段。
- [ ] **L-TEXT-1** [TextCleaner.java:25-26](travel-java/src/main/java/org/example/traveljava/util/TextCleaner.java#L25-L26) `"[[\\u0000-\\u001F]]"` 正则匹配的是字面文本非控制字符区间 → Unicode 转义残留清理是死代码（无危害，真控制字符已被 CONTROL_CHARS 覆盖）。
- [ ] **L-COORD-1** [CoordinateUtil.java:121-149](travel-java/src/main/java/org/example/traveljava/util/CoordinateUtil.java#L121-L149) from/to 传非法值（"wgs"/大小写错）时 default 分支静默当 gcj02 返回原坐标（偏差数百米无报错）。
- [ ] **L-COMMENT-1** [CommentService.java:157-172](travel-java/src/main/java/org/example/traveljava/service/CommentService.java#L157-L172) 删除顶级评论不级联删子回复 → 孤儿回复 UI 不可达但计入 comments 计数，列表页评论数与可见数永久不符。
- [ ] **L-UPLOAD-2** [FileUploadController.java:59](travel-java/src/main/java/org/example/traveljava/controller/FileUploadController.java#L59)、[:119](travel-java/src/main/java/org/example/traveljava/controller/FileUploadController.java#L119) 控制器允许 1GB 视频但 `multipart.max-file-size=50MB` 在框架层先拒 → 1GB 上限不可达，用户得到框架异常而非业务提示。

## 十五、低危 - agent-service/Python（4 项）

- [ ] **L-PY-1** [agent/planner.py:196](agent-service/agent/planner.py#L196)（+[agent/knowledge.py:184-192](agent-service/agent/knowledge.py#L184-L192)）每个请求重建知识库（读盘 + TF-IDF 索引）同步跑在事件循环上；knowledge.py:192 已备全局单例却不用。
- [ ] **L-PY-2** [agent/tools.py:414-434](agent-service/agent/tools.py#L414-L434) calculate_budget：days 有防御但 budget_total/people/分项没有 → LLM 回传 `"5000"` 字符串时 `"5000"*2` 与 int 比较抛 TypeError（MCP 暴露路径未捕获）。
- [ ] **L-PY-3** [agent/auth.py:67-89](agent-service/agent/auth.py#L67-L89) JWT 解码不强制 `exp`（无 exp 即永不过期）；aud/iss 默认不校验。
- [ ] **L-PY-4** [agent/mcp_bridge.py:173-189](agent-service/agent/mcp_bridge.py#L173-L189) `_started` 在 `t.start()` 即置 True，run() 在线程内异步失败（端口冲突）后线程静默死亡 → `/api/agent/health` 永远误报 running。

## 十六、低危 - 前端（35 项）

**地图/页面状态**
- [ ] **L-MAP-3** [TripMapView.vue:329-337](trval-h5/src/views/TripMapView.vue#L329-L337) initBaiduMap 对 Leaflet 实例调 `centerAndZoom`（BMapGL 方法）无 provider 校验 → 离线模式 + 百度 provider 时 TypeError 地图卡死。
- [ ] **L-MAP-4** [TripMapView.vue:917-918](trval-h5/src/views/TripMapView.vue#L917-L918) 语音识别读 `store.state.budget/days`——store 顶层无此字段（实际在 state.params）→ 语音填参恒空，行程预算/天数总走后端默认值。
- [ ] **L-MAP-5** [TripMapView.vue](trval-h5/src/views/TripMapView.vue) 城市标签 overlay 每次切换城市叠加一个，clearMapMarkers 只清 marker 不清标签 → 多城市反复切换标签越叠越多。
- [ ] **L-MAP-6** [AgentMapView.vue:857](trval-h5/src/views/AgentMapView.vue#L857) `(h.pricePerNight*tripDays)?.toLocaleString()`：pricePerNight 缺失时 NaN.toLocaleString()="NaN"（可选链救不了 NaN）→ 「共 3 晚 ¥NaN」。
- [ ] **L-CAL-1** [TripCalendarView.vue:26-31](trval-h5/src/views/TripCalendarView.vue#L26-L31) `new Date(非法串)` 不抛异常返回 Invalid Date（try/catch 捕获不到）→ 「NaN月NaN日 · 星期undefined」。
- [ ] **L-CAL-2** [TripCalendarView.vue:46-49](trval-h5/src/views/TripCalendarView.vue#L46-L49) `bd?.total || bd?.accommodation ? ... : null` 优先级错：只有 accommodation 无 total 时显示「预算 ¥0」而非隐藏。
- [ ] **L-TRIPS-1** [TripsView.vue:311-312](trval-h5/src/views/TripsView.vue#L311-L312)、[:69-74](trval-h5/src/views/TripsView.vue#L69-L74) keep-alive 首挂 onMounted+onActivated 相继触发 → 4 个加载函数各发双份请求；loadCarouselImages 只在已登录 onMounted 调 → 游客先开页、别处登录再返回时轮播永不加载且 `% 0` 使 currentCarouselIndex 变 NaN 后轮播永久失效。
- [ ] **L-TRIPS-2** [TripsView.vue:91-99](trval-h5/src/views/TripsView.vue#L91-L99)、[:124-141](trval-h5/src/views/TripsView.vue#L124-L141) 非安全上下文（http://IP 访问，线上即是）navigator.geolocation 不存在 → locateUser resolve(false) 且不设 userLocation → initNearbyMap 读 `userLocation.value.lng` TypeError，全屏地图初始化中断。
- [ ] **L-DEST-1** [DestinationDetailView.vue:181-183](trval-h5/src/views/DestinationDetailView.vue#L181-L183)、[:425-430](trval-h5/src/views/DestinationDetailView.vue#L425-L430) 无 city 参数时 `router.replace` 是异步导航，组件仍完整执行 onMounted（空城市请求 + 建地图实例）后才被替换走。应先 return。
- [ ] **L-TRIPMAP-1** [TripMapView.vue:1090-1095](trval-h5/src/views/TripMapView.vue#L1090-L1095) onUnmountedCleanup 定义后从未调用（死代码，逻辑已内联，无实际影响——顺手清理）。

**首页/规划/进度页**
- [ ] **L-HOME-1** [HomeView.vue:145-179](trval-h5/src/views/HomeView.vue#L145-L179)（+404-408、858-860）defaultBanners/defaultDestinations/promotionSlides/seedNotes 等在 setup 阶段（staticImageMap 为空时）一次性求值，JSON 加载完成后不更新 → 「静态图优先」失效，API 不可用时首屏全裂图。
- [ ] **L-HOME-2** [HomeView.vue:195-288](trval-h5/src/views/HomeView.vue#L195-L288)（+555-597）seedNotes 缺 isLiked/likeCount/commentCount → 兜底模式下 `likeCount += 1` 得 NaN（当前模板未绑入口，复用即触发）。
- [ ] **L-HOME-3** [HomeView.vue:834-837](trval-h5/src/views/HomeView.vue#L834-L837) addWheelListeners 的 500ms setTimeout 不随弹窗关闭取消 → 关闭后监听挂到已关 popup 残留。
- [ ] **L-PLAN-1** [PlanningView.vue:729-749](trval-h5/src/views/PlanningView.vue#L729-L749)（+852、903-923）debounceMap 300ms 定时器卸载不清理 → 卸载后仍对已卸载组件发 N 个图片请求。
- [ ] **L-PLAN-2** [PlanningView.vue:1314-1316](trval-h5/src/views/PlanningView.vue#L1314-L1316) connectStreamDetail 的 200ms setTimeout 卸载不取消（此时 detailAbort 尚 null）→ 卸载后才发 SSE 且无人 abort。
- [ ] **L-PLAN-3** [PlanningView.vue:1192-1198](trval-h5/src/views/PlanningView.vue#L1192-L1198) chatHistory 已含原始 text 又 push 上下文版 → 每次提问发两条连续 user 消息（浪费 token + 模型困惑）。
- [ ] **L-PLAN-4** [PlanningView.vue:1035](trval-h5/src/views/PlanningView.vue#L1035) copyPlan 直接 `structuredPlan.value.dayPlans.forEach` 无判空（709/其他处都有 `?.`）→ data.dayPlans 缺失时点「复制行程」TypeError 无响应。

**聊天/视频/社区/笔记**
- [ ] **L-CHAT-1** [ChatView.vue:336](trval-h5/src/views/ChatView.vue#L336) `if (e?.name === 'AbortError') return` 直接返回，isStreaming 保持 true（该页不在 keep-alive，onDeactivated 兜底不触发）→ 流中离开后 AI 气泡永久带 streaming 闪烁样式。
- [ ] **L-VIDEO-1** [CommunityView.vue:329-332](trval-h5/src/views/CommunityView.vue#L329-L332) previewImages 先 filter 掉视频再传含视频索引的 startIdx → 预览起始图错位一位。
- [ ] **L-VIDEO-2** [VideoDetailView.vue:684](trval-h5/src/views/VideoDetailView.vue#L684) 删除按钮 `v-if="getToken()"` 只判登录未判归属（NoteDetailView 用 currentUserId===c.userId）→ 别人评论也显示删除按钮。
- [ ] **L-NOTE-1** [NoteDetailView.vue:465](trval-h5/src/views/NoteDetailView.vue#L465) `v-for :key="opt.name"` 但元素只有 key 字段 → 6 个节点 key 全 undefined（应为 opt.key）。
- [ ] **L-NOTE-2** [NoteDetailView.vue:276-285](trval-h5/src/views/NoteDetailView.vue#L276-L285) 删除顶级评论只 filter 该条，子回复成孤儿静默不可见且 comments 计数只减 1。
- [ ] **L-NOTE-3** [NoteDetailView.vue:392-411](trval-h5/src/views/NoteDetailView.vue#L392-L411)（+219-236）骨架加载期间底栏可交互，`likeNote(undefined)` 发无效请求再回滚提示失败；VideoDetail 的 handleSendComment 同样未守卫 current.id。
- [ ] **L-WRITE-1** [WriteNoteView.vue:177-198](trval-h5/src/views/WriteNoteView.vue#L177-L198) 编辑模式加载详情无 loading/竞态守卫 → 响应晚到整体覆盖 noteForm，用户已输入内容被清掉。

**组件/工具层**
- [ ] **L-COMP-1** [DragSheet.vue:58](trval-h5/src/components/DragSheet.vue#L58) SNAP_POINTS 在 setup 一次性快照 → 后续 min/mid/maxHeight props 变化不更新吸附点。
- [ ] **L-COMP-2** [i18n/index.js:58-62](trval-h5/src/i18n/index.js#L58-L62) 英文包加载失败（返回 null）仍执行 applyLang('en-US') 并持久化 → 与注释「失败回退中文」不符，之后每次启动逐 key 回退且重试拉取。
- [ ] **L-COMP-3** [security.js:91-97](trval-h5/src/utils/security.js#L91-L97) `url.startsWith('/')` 判断在 `/api/proxy/image` 判断之前 → 后者死分支，「已是代理地址原样返回」从未生效。
- [ ] **L-COMP-4** [SearchBar.vue:110-121](trval-h5/src/components/SearchBar.vue#L110-L121) 底部「收藏/标签页」筛选按钮只切换高亮不过滤列表 → 死 UI 误导。
- [ ] **L-COMP-5** [VirtualList.vue:166](trval-h5/src/components/VirtualList.vue#L166) 整个列表容器 `aria-hidden="true"` 把全部内容对屏幕阅读器隐藏（可访问性误用）。
- [ ] **L-COMP-6** [chatSession.js:107-113](trval-h5/src/utils/chatSession.js#L107-L113) 查询型 getCurrentSessionId() 无会话时调用 createNewSession() → 副作用批量制造空会话记录（多标签页/注销后残留调用）。
- [ ] **L-COMP-7** [AIChatDialog.vue](trval-h5/src/components/AIChatDialog.vue) 组件级遗留：滚动节流等已确认干净，此项留作修复时顺带检查 quickWithContext 与普通 send 的上下文统一（配合 FEAT-12）。

---

## 附：审查确认干净的部分（无需复查）

- axios token 刷新：单飞 refreshPromise + refreshedOnce 单次重放 + AUTH_EXEMPT_URLS 防死循环，无竞态
- i18n：zh/en 全量 key 比对互相缺失 0；静态 t() 调用未定义 key 0
- GET 去重（key 含 params、TTL 200ms）、重试（仅 GET 网络错误/5xx、退避 300/900ms）实现正确
- 文件上传路径穿越防护（resolve+normalize+startsWith）、服务端生成落盘文件名
- 游记/评论/关注/订单等归属校验（服务层 getOwned）；TripTemplateRepository 搜索为参数绑定无注入
- JWT 密钥 fail-fast；管理员默认口令 fail-fast（AdminInitializer:47，已亲自核实，非 bug）
- 支付回调验签 fail-closed；mock-pay 有属主校验 + 开关；OrderController 裸创建/改价老漏洞已移除
- RefreshToken 旋转用 getAndDelete 原子操作；V3 六张新表与实体列/类型/索引完全一致；Repository 派生查询方法名全部匹配
- AmapService 其余接口均已正确 URI 传参（无二次编码遗漏）；VoiceToTextService/MapService/HotelService/ContentModerationService 未发现问题
- 前端：ChatView 4 个定时器与 visualViewport/resize 解绑正确；NotesView/CommunityView 首屏 useRequest 守卫有效；markdown 渲染 html:false 无 XSS；v-for key 稳定

## 附：已剔除的审查误报（4 项）

1. 「admin/admin123 弱口令」——AdminInitializer 启动 fail-fast 拒绝默认口令，已核实代码
2. 「CityDTO/TextCleaner 含非法正则字符」——审查工具显示层替换伪影，字节级验证无问题
3. 「TripTemplateRepository like 注入」——JPQL 参数绑定
4. 「ChatView SSE 清理缺失」——AbortController 正确传入且 onUnmounted 清理完整
